# CharSheet Backend

**sql.js** — pure JavaScript SQLite, працює на Windows/Mac/Linux без компіляції.

## Запуск

```
cp .env.example .env
# відкрий .env і встав JWT_SECRET
npm install
npm start
```

Файл `charsheet.db` створиться автоматично.
